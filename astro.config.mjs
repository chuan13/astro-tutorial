// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    // deploy to Github Pages
    site: 'https://chuan13.github.io',
    base: '/astro-tutorial'
});
